import {
  create,
  NButton,
  NIcon,
  NTooltip,
  NLayout,
  NLayoutSider,
  NMenu,
  NInput,
} from "naive-ui"

const naive = create({
  components: [
    NButton,
    NIcon,
    NTooltip,
    NLayout,
    NLayoutSider,
    NMenu,
    NInput,
  ]
})

export default naive